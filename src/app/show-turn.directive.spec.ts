import { ShowTurnDirective } from './show-turn.directive';

describe('ShowTurnDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowTurnDirective();
    expect(directive).toBeTruthy();
  });
});
